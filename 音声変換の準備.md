# Python（音声変換）
- コマンドプロンプトを起動
- pythonファイルがあるディレクトリまで移動
  - 音声変換用のファイルが入っているフォルダを左クリック→「パスのコピー」
  - コマンドプロンプト上でディレクトリを移動
    ```
    cd {コピーしたパス}
    ```
- pythonファイルの実行
  ```
  python　{pythonファイル名}
  ```
